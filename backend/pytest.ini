[pytest]
markers =
    realdata: marks tests that require access to production-like assets stored outside the repo
    slow: marks tests that are intentionally slow due to full multimodal model execution
